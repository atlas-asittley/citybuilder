# Archive

Historical operational documents kept for context. Anything in here is **no longer actionable** — git history preserves the full lineage if you need it.

- **`M1_M2_deployment_runbook.md`** — instructions used to deploy M1 (district scaffolding) and M2 (distance-based resource collection) to the live Supabase project. The migrations themselves are tracked in the regular `city-builder-mvp/*.sql` set; this is the one-time runbook that walked through running them.
