# City Builder TODO

Running list of things to do. The user (Atlas) adds items; we work through them
together. Move completed items to **Done** with a date so the history is
visible without needing `git log`.

Order is build-order: small fixes first, foundations next, economy retuning
after that, polish, then big design lifts last.

---

## Up next

- [ ] **Civic Metrics Expansion — watch & tune (all phases LIVE 2026-05-28).** The whole expansion shipped to `main` + the live DB (6 migrations applied in order; 38 server + 170 frontend tests green; live tick verified against Jill's 1,164-bldg city). See **Done** below for what landed. Remaining is watch + the deliberately-staged "toothless" flips:
  - **Watch on live:** Jill's waste is 100 → −8 desirability drag; congestion 100 → −8% productivity (both bounded, recoverable). Watch for borderline-house devolution and whether the new pressures feel fair. Tune magnitudes if needed.
  - **Staged flips (each a documented one-liner, flip after watching):** (a) **Noise → desirability**: add `- LEAST(10, mt.noise)` in `_pp_update_desirability`. (b) **Food-variety gate**: have `_pp_evolve_housing` read `housing_tier_config.food_variety_required` vs count of distinct in-stock `is_food` resources (the real iron-monopoly fix — flip carefully, can strand cities).
  - **Balance watch:** road desirability auras SUM across overlap (radii kept small); revisit if cheap paved roads trivially max desirability.
  - **Optional refactors (flagged, not done):** server orchestrator copy-paste (`_pp_for_uid` re-copied per phase → extract `_pp_update_metrics`/`_pp_metrics_json`); frontend per-category metadata spread (~7 files → a registry). See `feedback_migration_gotchas` #5b.
  - **Note (pre-existing, unrelated):** the full suite has 2 red on `main` today — `test_food_tiles::test_non_food_building_rejected_on_food_tile` (reject trigger drifted) and `test_procedural_traders::test_procedural_trader_has_3_to_6_resources` (stale 3–6 assertion vs the black-market full-pool change; count is now ~27–31). Both reproduce with zero waste code loaded. Worth a separate cleanup.

- [ ] **Verify perf #1 on Jill's phone (mobile heat).** Shipped `dff4bfe`: IntersectionObserver tags `.bldg.bldg-offscreen` when out of map viewport; CSS hides decorative `::before` / `::after` (smoke, glow, figures) for those via `display: none`. Building sprite itself stays painted. Don't ship perf #2 until Jill confirms (a) no rendering glitches on zoom/scroll, (b) heat/battery clearly improved on her Safari.
  - **Code re-verified 2026-05-10**: implementation intact; the only path that creates `.bldg` DOM is `renderMap → grid.innerHTML → observeBuildings()`. No subsequent commits introduced incremental bldg insertions that would bypass the observer.
  - **For Jill's verification:**
    1. Open the game on her iPhone Safari and play for a few minutes (place buildings, scroll/zoom around).
    2. Confirm no zoom/scroll glitches: building sprites should stay painted, smoke/glow/figure decorations should appear cleanly when scrolled back into view (no half-frames).
    3. Optional sanity: open Safari Web Inspector → Console, type `__perfStatus()`. Should return e.g. `{total: 87, paused: 32, ratio: "37%"}` after scrolling away from the main cluster. If `paused: 0` no matter what, perf #1 isn't active and we need to debug.
    4. Heat/battery: subjective phone-feel comparison vs. her recollection of pre-2026-05-08 sessions.
- [ ] **Verify perf #2 on Atlas's phone (mobile lag).** Shipped `7ff1c80` 2026-05-11. Replaced the 9 infinite `act-glow` loops with static `opacity: 0.65` plus a one-shot `act-tick-glow` pulse fired from `processProduction` (every ~30s client poll). Atlas should confirm: (a) the affected buildings still feel "alive" — they pulse briefly each tick instead of glowing constantly — and (b) the game feels noticeably less laggy / phone runs cooler. Affected: iron_mine, smelter, glassworks, foreman_office, mine_office, mosaic_workshop, distillery, brewery, watch_house. Don't ship another perf change until verified.
- [ ] **Trader reputation system.** Atlas mentioned during the 2026-05-09 partners-tab redesign: as a player trades more with a specific partner, their relationship should improve and grant some advantage — possibly preferential prices, larger quotas, faster visit cadence, or an exclusive "VIP" goods unlock. Open: which axis to scale (price multiplier vs quota vs cadence vs unlocks)? Per-(player, trader) state, decays with inactivity? Player-facing rep number on the trader card? Sketch a small design doc when picking this up. The existing `trader_relationships` table got dropped on 2026-05-08 with the missions removal, so we'd reintroduce a fresh table tailored to this design rather than restoring the old one.
- [ ] **Multi-input recipes — break the linear chains.** Right now every processor takes one input and produces one output (timber → lumber → furniture, clay → pottery, etc). Add buildings that need 2+ different resources to make something (e.g., a tools workshop that needs timber + iron, a windowed-furniture line that needs lumber + glass). Goal: weave the industry trees together so cross-industry trade and combinations matter. Open: schema change to building_types (multi-input column or a new building_inputs table?), recipe display in the UI, balance pass, and which specific recipes/buildings to add. Multi-session feature — design doc first.
- [ ] **Water terrain (rivers / lakes / ponds) + research + bridges.** Multi-part feature. (a) Schema: extend `map_tiles.terrain_type` with `river`, `lake`, `pond`. Generation: lay water tiles at chunk-allocation time — rivers cut through districts (cross-chunk continuity), lakes/ponds are localized blobs. Tag them `buildable = false`. (b) Research system (NEW — doesn't exist yet): per-player tech tree with prereqs, costs (probably $$$ + a new `research_points` resource), and unlocks (new building keys / bridges / ?). Need separate design doc — open questions: research generated by buildings (libraries/universities) or by population/time? Tree depth, cost curve, gating mechanics. (c) Bridges: new building category that overlays a road on a water tile. Costs more than a regular road. Available only after the bridge research. UI: bridges show in the build panel as a sibling of road. — This is several sessions of work; design doc first before any code.

---

## Done

- **2026-05-28** — **Civic Metrics Expansion (all 5 phases, LIVE).** Added new civic dimensions, fixed the dead-resource problem, and began equalizing the four industries. Source of truth: `citybuilder-game/docs/CIVIC_METRICS_EXPANSION.md`. Shipped to `main` (both repos) + 6 migrations applied to the live DB in order (waste → power → brownout → roads → noise_congestion → health_education).
  - **Waste** 🗑 (per-player pressure; `sanitation` buildings Dump/Recycling/Incinerator; bounded −8 desirability drag). **Power** ⚡ (capacity vs demand; Watermill/Windmill/Powerhouse; brownout electrified-only). **Fancier roads** (Paved/Tiled/Grand Boulevard → desirability auras + relieve congestion). **Noise** (per-tile, heatmap, toothless) + **Congestion** 🚗 (traffic vs road capacity, bounded −8% productivity). **Health** ❤️ + **Education** 🎓 (Clinic/Library, upside-only productivity). **Food-variety** column staged (toothless).
  - **Dead-resource fix landed:** machinery → Incinerator/Powerhouse; monuments+cabinets+mosaics → Grand Boulevard. All four T4 capstones now have real sinks. Charcoal (timber) → Powerhouse fuel; lumber+glass (timber/clay) → Clinic/Library.
  - 38 server tests (`test_{waste_management,power_energy,fancier_roads,noise_congestion,health_education}.py`) + 170 vitest green. Live tick dry-run verified against all real players. Player changelog published (`civic-metrics-expansion`). Two pre-existing unrelated reds remain on main (food_tiles reject trigger; procedural_traders 3–6 count).

- **2026-05-21 (overnight)** — Maintenance pass. Verified all v2 sprites use correct %-encoding (no more gray-square fallbacks); added a vitest regression that catches the bare-`%` shape (`<digit>%<non-hex>`) in any sprite data URI. Added pytest regression for civic-category staffing (drives a Public Garden through `process_production` and asserts `is_staffed=true` — would have caught the 2026-05-22 omission of `'civic'` from `_pp_staff_buildings`'s category IN list). Updated `test_drain_proportional_across_food_resources` for the 2026-05-21 bread-demand halving (T5 bread lifestyle 0.10 → 0.05/min). Two new memory entries: `feedback_new_building_category_checklist.md` (7-place checklist for any new `building_types.category` value) and `feedback_svg_data_uri_pct_encoding.md` (encoding rule + verification grep). Bug inbox is at 0.
- **2026-05-21** — Civic-amenity category (3 new buildings: Marketplace, Hospital, Industrial Zone; on top of morning's Public Garden + Monument). Each follows the "solve a problem AND create a small problem" pattern: Marketplace +trader-sell% but adds crime; Hospital city-wide crime reduction but competes for ale; Industrial Zone bigger booster radius but pollutes. New schema columns: `crime_emit`, `crime_reduction`, `trade_sell_bonus_pct`, `migration_bonus`. `_pp_update_population` reads migration_bonus; `compute_crime` sums emit and subtracts reduction; `_rtv_sell_phase` applies marketplace bonus capped at +25%. 5 SVG sprites (mill-grade detail). FE wired into BuildTabPanel + InspectorPanel + helpers + MainScene tints. After morning ship, follow-up patch added `civic` to `_pp_staff_buildings` and `_pp_workers_needed` (silent inertness bug Jill caught within hours).
- **2026-05-18** — Bread lifestyle gate accepts substitutes. Atlas: "the bread requirement is steep — spices, caviar, and spirits (or any combination) should also suffice." New `lifestyle_substitutes(primary_key, substitute_key)` table seeded with bread → {spices, caviar, spirits}. `_pp_refill_pantries` now mirrors the food-branch pool pattern for any demand with substitutes — drain proportionally across primary + substitutes when refilling the bread pantry. `_pp_evolve_housing` upgrade gate accepts any substitute as satisfying the named demand. Devolve gate unchanged (still reads per-house buffer; buffer fills from substitutes so the gate already works). v2 inspector + runway calc updated to mirror server behavior; v1 inspector + state loader also updated. 6 new regression tests in `test_lifestyle_substitutes.py` (all green); 425/426 of the wider suite still green (1 unrelated flake in `test_parcel_resource_density`). Changelog entry shipped.
- **2026-05-10** — Maintenance pass. (a) Audited every `sb.from(...).select()` for the 1000-row server-cap bug (it bit us twice — Treasury chart + Max's map). Extracted `fetchAllPaged` into `js/paginate.js` and routed 5 latent landmines through it: inspector_tile reloadAfterTileChange, inspector_building post-upgrade refetch, map.js reloadMapData, game.js evolution-events handler, reports.js fetchTradeFlows. The fetchTradeFlows site had a no-op `.range(0, 9999)` against the server cap — would have started silently truncating Drew's panel within days at his current ~750 rows / 7d rate. (b) Full pytest sweep + TODO sync. (c) Memory entries reviewed and stale entries cleared.
- **2026-05-09** — Starter parcel: guarantee one food tile adjacent to a road. New helper `_guarantee_food_near_road` runs after roads + clusters in `allocate_district_chunk`, but only on first-chunk allocations. If no existing food tile is road-adjacent, picks a random empty road-adjacent tile in the parcel and stamps the food key on it. Existing parcels untouched. 2 regression tests across all 4 industries × 5 trials each.
- **2026-05-09** — Map fix: paginated map_tiles + buildings fetch in loadGameData. Atlas reported Max joined and "only half her parcel is there" — Supabase enforces a server-side `db-max-rows=1000` that overrides client `.range()` requests. World had 1125 tiles when Max joined; her highest-y tiles got chopped. New fetchAllPaged helper loops in 1000-row pages until a short page arrives.
- **2026-05-09** — Doubled resource density on new parcels. `allocate_district_chunk` now seeds 2× the cluster count for both industry resources and food tiles (walk lengths unchanged, so patches stay the same average size — there are just twice as many per chunk). First-chunk industry 2→4 clusters, food 1→2 clusters. Existing parcels not touched.
- **2026-05-09** — Trader-cap reset clock. New 🔄 pill in the topbar shows time-until-next-UTC-midnight (when trader_daily_quota's day_bucket rolls over). Cheap per-minute setInterval refresh.
- **2026-05-09** — Per-house pantry buffers replace global-pool consumption. Atlas's design call: "you would think that a house would devolve after its supply of that resource runs out [...] not as soon as there's not enough of a particular resource for them to stay at the evolution at the rate." New `building_resource_buffers` table — every house holds 30 minutes of consumption per gated resource (food + lifestyle goods: pottery / bread / furniture / statuary). Each tick the consume phase decrements per-house, then refill phase tops up from city stock proportional to deficits. Devolve gate now reads the building's OWN buffer, not city stock — so "city pottery hits 0" no longer cascades all houses into devolve at once; they trickle out one-by-one over ~30 min as their pantries empty. Triggers keep buffers in sync with placement / tier upgrade / devolve / demolish; migration seeded existing houses at full capacity. Inspector surfaces fill ratio per resource. Treasury chart aggregation also moved server-side (1000-row cap fix). 8 new pantry regression tests + 5 housing-test updates. Memory entry added.
- **2026-05-09** — Treasury chart aggregation moved server-side (`get_treasury_daily_series` + `get_cash_ledger_by_source` RPCs). PostgREST's 1000-row default was silently truncating Drew's chart (1571 rows in trailing 7 days). Date axis added under both Daily-net bars and Cash-balance line. 8 tests covering auth, bucketing, cross-midnight splits, the >1000-row case, source/sink jsonb, ledger group-by, and player isolation.
- **2026-05-09** — Player-facing changelog. New `changelog_entries` table (slug, title, body, published_at) + `player_profiles.last_changelog_seen_at` watermark. On game load `get_unseen_changelog_entries()` returns rows newer than the watermark; the modal in `js/changelog.js` pops them up newest-first. First-time players (NULL watermark) see only the most recent so they don't get a wall of historical context. "Got it" calls `mark_changelog_seen()` to bump the watermark. Settings → "What's new" shows the full history (read-only, no watermark bump) via `list_changelog_entries(p_limit)`. Seed entry covers the trade redesign + reservation prices. 7 regression tests + memory entry `feedback_changelog_publishing.md` documenting the workflow for future migrations: drop a row in the same SQL that ships a player-visible feature.
- **2026-05-09** — Trade Partners overhaul + reservation prices. Replaced the per-trader tab strip with a vertical scrolling list of collapsible trader cards. Added per-resource reservation prices (`min_sell_price` / `max_buy_price` on `trade_policies`) gated server-side in `_rtv_sell_phase` / `_rtv_buy_phase`. Added "Your price gates" banner showing which partner currently meets each gate. Locked partners are no longer listed by name (open-ended pool, not a fixed roster). Two new optional inputs on City → Resources policy block. 5 regression tests in `test_reservation_prices.py`. TODO entry added for trader reputation system.
- **2026-05-09** — Overnight intense audit. Atlas brief: "find anywhere the calculations don't match... take hours and hours." 9 phases. Test count 342 → 366. (a) **5 cross-player auth holes fixed.** `reset_player(p_player_id)` was a total account-takeover vector; allocate_district_chunk / place_pre_road / recompute_extractor_paths could all be called for other players. Plus dropped INSERT/UPDATE/DELETE RLS policies on buildings/player_profiles/inventories/trade_policies/trade_agreements/trader_visits/trade_transactions — direct UPDATE bypassed all server logic (free $50k airport upgrades, money cheat, etc). (b) **black_market_trade was broken in production** — every call hit a FK violation because no 'black_market' row existed in the traders table. Fixed. (c) **Phantom labor shortage** — _pp_workers_needed counted transport buildings (33+ workers) but they were never staffed. Removed transport from the count. (d) **Multi-player trader unlocks miscounted** — client used current-player road access to count other players' hubs; never tallied them. (e) **client trader_prices collided city + global rows** for randomized catalogs. (f) **Demolish + cheat-code bypassed cash ledger** — converted to RPCs (demolish_building, dev_grant_money). (g) **Race condition in process_production** — two simultaneous ticks for one player would double-drain food. Added FOR UPDATE serialization. (h) **Choose_industry's $1000 starting grant** had no cash_transactions row — Treasury ran $1000 short of reality. Fixed + backfilled. (i) Dead tables `counter` + `trade_offers` dropped. (j) 24 new regression tests covering the new RPCs, RLS lockdown, cross-player guards, race conditions, and ledger invariants. Memory: project_overnight_audit_2026_05_09.md.
- **2026-05-08** — Overnight deep audit. (a) Integer-ratio recipes everywhere goods are labeled in the UI — extracted shared `recipe_format.js` module; help/inspector/build-panel all use it. (b) Database integrity sweep: 36 FK constraints clean, RLS gaps locked on 3 dead tables, 4 numeric-bloat columns rounded (population/migration_rate/happiness + every food/processor inventory column). (c) Three precision-fix migrations now cover every multiplicative inventory update. (d) Cash ledger reconciliation: identified a ~$17k gap per player from auto-trader and 5 P2P money paths that bypassed `cash_transactions`. Filled all gaps and added ledger_adjustment backfill so books balance going forward. (e) Daily-cap-aware trade rate projection — runway pill now uses sustained rate (cap/1440min) not burst rate (visit_capacity/interval). (f) Runway pill staleness: hooked `updateCityRunway` into all build/demolish/pause/priority/trade actions. (g) New regression test suite for the precision bug class (4 new tests, schema-wide invariant catches future writers that forget to ROUND). 339/342 tests passing; 3 documented flakes unrelated.
- **2026-05-07** — City runway indicator (commits `15002be` + `c97ae51`). Topbar pill (⏳ Xh Ym) showing time until the first devolve-triggering resource depletes. Aggregate food + each lifestyle good (pottery / bread / furniture / statuary) tracked independently. Tap → modal with per-resource breakdown sorted bottleneck-first. Also fixed `computeNetRates` (the engine behind the City → Resources Rate column) to include cumulative lifestyle drain — previously pottery/bread/etc. read as net-zero rate even when 16 Townhouses were depleting them at 1.2/min.
- **2026-05-07** — Cumulative + scaled housing lifestyle demands (commits `90c232f` → `211837a`). Lifestyle goods (pottery / bread / furniture / statuary) now stack across tiers and scale with worker count — a Manor row needs all four luxuries continuously, not just statuary. River Traders sells pottery so non-clay starters have a path. Inspector now lists ALL upgrade blockers + ALL devolve risks during the grace window (with a separate hint when a bathhouse is the only thing currently holding the tier). After Jill's 16 Townhouses devolved on the initial rates, halved every lifestyle rate (introducing tier 0.05/min, +0.025 per tier above) and restored Ironwood. Also fixed an unrelated unbounded-numeric-precision bug in the food drain (`quantity * factor` was accumulating ~20 digits per tick — Jill's bread row hit the 16383-digit ceiling). Tests caught up, 334/338 green (4 pre-existing failures unrelated to this work).
- **2026-05-07** — `js/inspector.js` split (commit `a1c9422`). 1046-line file → barrel + 5 modules: `inspector_core` (state machine, lifecycle, mode dispatch), `inspector_helpers` (shared lookups + housing tier-requirement gates), `inspector_building` (the big building-card renderer + actions), `inspector_tile` (resource-tile flow), `inspector_walker` (walker info card). Public API unchanged via the barrel. Verified by Atlas in browser.
- **2026-05-07** — Overnight maintenance pass. (a) Balance sandboxes shipped under `/home/atlas/citybuilder/sandbox/` — pure-Python `balance_sim.py` + DB-driven `db_sim.py` + scenarios + BALANCE_NOTES.md initial findings. (b) Multi-tile inspector highlight verified — single-tile cells use `.cell.inspected-source`, multi-tile use `.bldg.inspected-bldg` outset (where the multi-tile-anchor cell has `contain: none`). Both readers in map.js. (c) Test suite restored to 338/338 green from 45 initial failures, mostly via conftest defaults (`tutorial_done=True / population=100`) and per-test population overrides for tests that need tight worker pools. (d) Memory updated: `feedback_balance_invariants.md` now reflects floor=0/15/tutorial-aware + sticky trade_unlocked. New memories: `project_tutorial_v2.md`, `project_balance_sandbox.md`, `feedback_session_decisions_2026_05_07.md`. (e) `inspector.js` refactor DEFERRED — see Up next.
- **2026-05-06** — City + district names + two-row topbar. New `cities` table + `player_profiles.city_id` / `.district_name`. choose_industry takes optional `p_district_name` + `p_city_name` (city name only matters for the first-player flow when no city exists). All existing players backfilled to a default city named "Lyrandel" with district_name = display_name. Topbar split: row 1 = city › district + money + (settings/bell/help), row 2 = stat icons. New `rename_district` / `rename_city` RPCs wired through Settings modal. Industry-select form gains a District input always; City input only appears when no city exists.
- **2026-05-06** — Bottom-panel scroll fix. Full-size mode wouldn't scroll while half-size did — flex children defaulted to `min-height: auto`, refusing to shrink below content size. Added `min-height: 0` to `.bottom-panel`, `.bottom-panel.expanded`, `.panel-content`, `.trade-subpanels`. The chain from `.game-body` (already had it) → bottom panel → inner scroll containers is now correct top-to-bottom.
- **2026-05-06** — City tab consolidation. Top-level tabs are now Build / City / Trade (was Build / Inventory / Trade / Reports). Inventory tab + renderInventory() removed; the resource list lives under City → Resources with 7 collapsible categories (raw materials / processed / cross-industry / industrial luxuries / food staples / processed food / luxury food). Each row expands into a drilldown showing partner trade flow + inline NPC trade policy controls (mode select + reserve threshold). Trade-policy controls used to be buried under Trade → Partners; surfacing them per-resource makes the "sell only above N" mental model first-class. saveTradePolicy exported from panels.js so reports.js can call it. Black Market stays in Trade → Partners.
- **2026-05-06** — Multi-tile inspector highlight. The gold ring used to mark only one cell of a 2x2 building. Moved highlighting from `.cell.inspected-source` to `.bldg.inspected-bldg` — the .bldg element already spans the full footprint via absolute positioning + 200%/300% width/height, so a single outset box-shadow on it draws one clean rectangle around the whole building regardless of size. The resource-tile highlight (`.cell.inspected-target`) stays cell-based since it's on a non-building tile.
- **2026-05-06** — Topbar reorg (first pass). Renamed "chunks" → "parcels" everywhere player-facing (schema columns kept as `chunks_owned` / `district_chunks` — too invasive to rename). The "+ Expand" button moved into the parcels stat-info popup, where it shows the next-parcel cost and disables when you can't afford it. Reset + Logout collapsed into a Settings (⚙) modal, leaving topbar-right as just three icon buttons: Settings, 🔔 bell, ? help. doLogout / doReset live in auth.js as exports the modal calls.
- **2026-05-06** — Notification log + prune all toasts. New `js/notifications.js` module with localStorage-backed log (per user, capped at 50 entries, dedup-collapse for identical events within 1.5s). 🔔 bell button in topbar with unread badge; click opens dropdown of recent events. `showToast()` is now a thin shim that forwards to `addNotification()` so every existing call site routes to the log. Dropped the production-tick "+N goods produced" toast and the "Tap any building" first-load hint entirely. Housing upgrade/devolve switched to log-only (uses full tier names — no more "V devolved to T"). Bankruptcy + crime + incoming trade offer continue to log. Dropdown pinned to `right: 8px` of viewport so the 320px panel doesn't slide off the left edge on phones.
- **2026-05-06** — Productivity v2: 3 more levers added to `_pp_compute_productivity`. **Education coverage** (+0.03 per 10% of tier-1+ houses within 5 tiles of a staffed school, max +0.10). **Tools stockpile** (+0.10 if tools ≥ pop·0.5, +0.05 if tools ≥ pop·0.2 — soft only, tools NOT consumed in v2). **Worker buffer** (-0.05 when workers_used ≥ worker_capacity, guarded against fresh players with capacity=0). Sum capped to ±0.30, then 1.0+sum clamped to [0.7, 1.3]. Net swing now -0.15 to +0.25. Topbar tooltip updated to mention all five levers. Design doc closed — all 4 open questions resolved per the doc's recommendations. 337/337 tests green (9 new lever-specific tests).
- **2026-05-06** — panels.js split → reports.js. Treasury + Resources sub-panels (renderTreasuryPanel, renderResourcesPanel + their helpers — fetchTradeFlows, aggregateTradeFlows, fetchCashLedger, fetchDailySeries, renderTreasuryAdvisor, renderDailyBars, renderBalanceLine, renderFlowBars, prettySource, period-toggle, replaceLoading, resIconClass, formatRate) extracted into `js/reports.js`. panels.js dropped from 1821 → 1268 lines. computeNetRates + resourceName exported from panels.js so reports.js can import them. renderSubpanel dispatch updated to import the renderers from reports.js. 'reports' added to the cache-busting module list in index.html. 328/328 tests green.
- **2026-05-06** — Productivity v1 shipped. Per-player multiplier on production output (extractor / food extractor / processor / tax). v1 has two levers: crime drag (-0.005/point above 50, max -0.10) + staffed-tavern bonus (+0.05). Stored on `player_profiles.productivity`, returned in process_production JSON. New ⚒ topbar indicator + stat-info popup. 6 specific tests pass. v2 levers (education / tools / worker buffer) deferred — see TODO.
- **2026-05-06** — Desirability v2: housing tier gate is now active. Upgrade gate (must meet next_tier.min_desirability) AND devolve gate (with 30-point hysteresis to protect existing housing on borderline tiles). Session GUC `city.skip_desirability_gate` lets test fixtures bypass; production never sets it. 4 new gate-specific tests verify upgrade-blocked / upgrade-succeeds / devolve-fires / hysteresis-protects.
- **2026-05-06** — Bottom-panel reorganization: Trade split into Trade + Reports. Trade now has 3 sub-tabs (Partners / Missions / Players); a new top-level Reports tab holds Treasury + Resources. Mental model becomes "people stuff" vs "money stuff" instead of "Trade contains everything financial." Existing sub-panel IDs unchanged so no other JS rewires; sub-tab click handler scoped to its local container so each parent panel remembers its own active sub-tab.
- **2026-05-06** — Desirability v1: per-tile score (0-100) collapsing pollution + crime + tax + service coverage into one number. Computed each tick, stored on `map_tiles.desirability`. Surfaces in the housing inspector ("Desirability: 72/100 — qualifies for Villa") and as a new green-to-red heatmap mode. Tier thresholds populated on `housing_tier_config.min_desirability`; the actual housing-eval gate is deferred to v2 (same playbook as pollution v1 — let players see the score and react before flipping consequences). 318/318 tests green.
- **2026-05-06** — Heatmap overlay menu. The 🌫 single-toggle button became a 🗺 popup with four mode options: Pollution (yellow per-tile), Crime risk (red on uncovered housing, green on covered), Building issues (red outline on idle/unstaffed/disconnected/paused), Desirability (green-to-red gradient). Mutually exclusive; mode persists in localStorage as `city_heatmap_mode`.
- **2026-05-06** — Pollution v1: per-tile score with industrial sources + park dampers, sprites, heatmap, inspector visibility. Visible-but-toothless (no housing gate yet, deferred to v2). New buildings: Park ($200, $3/min upkeep, dampens 8 within r3), Tree Grove ($120, $1/min upkeep, dampens 4 within r4). 7 emit tiers from 10/r4 (smelters/forges) down to 0 (services/housing/road).
- **2026-05-06** — Treasury Advisor: 4-component dashboard at the top of the Treasury sub-tab. Burn rate + runway projection ("-\$340/day · cash runs out in ~12 days"), top-source/top-sink chips, daily-net 7-bar chart (green up / red down inline SVG), cumulative-balance sparkline (color picks up trend direction). Plus: replaced the static "Income sources" / "Spending" tables with horizontal proportional bars whose width scales by share of the largest entry.
- **2026-05-06** — Walker perf: 60-walker soft cap + IntersectionObserver pause offscreen. Each building still has its own roster target; cap stops global growth past 60 with round-robin rotation so late-list buildings still get walkers. IntersectionObserver flips `.walker-offscreen` on each walker when it scrolls out of viewport; CSS pauses animation-play-state. ~70-80% of walker animations are paused at any moment on a typical phone viewport.
- **2026-05-06** — Trade fixes: debounce robustness + accept-zero-from-negative. (1) Trade-policy reserve-input timer now lives in a module-scope per-resource map; re-renders cancel cleanly and the latest typed value wins. (2) `accept_trade` no longer rejects when the counterparty has negative cash but receive_money = 0 — gated both balance checks on `> 0`.
- **2026-05-06** — visibilitychange listener leak fix. `initVisibilityPause()` was called from `enterGame()` which runs every reset, stacking duplicate listeners. Moved to `initGameEvents()` (one-time at app startup). Found during a deep memory-leak audit that confirmed everything else (walker DOM, renderMap innerHTML pattern, state arrays, realtime channel, modal lifecycle) was clean.
- **2026-05-06** — Cell perf: `contain: layout paint` + `content-visibility: auto` + `contain-intrinsic-size: 35px 35px`. Browser skips offscreen cells; per-cell layout/paint scopes prevent sibling reflow cascades. Multi-tile-anchor cells opt out (`contain: none`) so 2x2 sprites can overflow correctly.
- **2026-05-06** — Bottom panel three-state: hide / half / full, with two header buttons. Full-screen state hides the map entirely (`body.panel-expanded .map-area { display: none }`). Persists across reloads.
- **2026-05-06** — Symmetric population model + topbar migration indicator (↕). Replaces the asymmetric "snap up to housing capacity" with ±1 citizen/min cap, scaled by distance from happiness=50. Hard floor at population=5 (under-floor refill regardless of happiness — death-spiral safeguard).
- **2026-05-06** — Stat-info popups (workers / chunks / happiness / crime / migration). Tap a topbar stat → small dialog with What it measures / Why it matters / How to improve.
- **2026-05-06** — Recurring trade agreements between players. New `trade_agreements` table + 5 RPCs (propose / accept / cancel / list / `_pp_run_agreements`). Resolver is single-driver, fires at most one agreement per call (bounded backlog), skips when either side is short on stock or money.
- **2026-05-06** — Animations Pass 2 finished for the 9 buildings on a generic worker bob: distinctive motion via existing keyframes (act-glow, act-spin, act-buzz, act-sparks, act-bob).
- **2026-05-06** — Build panel sub-sections accordion (only one open at a time).
- **2026-05-06** — Build panel descriptions complete + accurate (cabinetmaker shows it needs lime, etc.).
- **2026-05-06** — Rebaselined `baseline_schema.sql` from live PG 17.6.
- **2026-05-06** — Balance pass after live-data game assessment. Six fixes addressing observed problems on Jill's deployed game (paid $32k upkeep on three unstaffed police buildings, crime stuck at 100). (A) `_pp_run_upkeep` now always advances `last_processed_at`, so a building going unstaffed and re-staffed doesn't pay for the unstaffed gap. (D) Staffing priority now tiers services + police above production categories so a worker shortage idles sawmills before it idles wells. (E) Police upkeep cut ~20% (WH 5→4, PS 12→10, Const 25→20) so 2 tax offices now cover 1 constabulary with headroom. (B) Build panel cost line includes `$N/min upkeep` for police. (C) Single-shot toast on the tick where money first goes negative. (F) Single-shot toast when crime crosses 70 going up. 312/312 tests green. Trade-dependency opacity (item 8) addressed indirectly — bankruptcy toast points players at Treasury, which already breaks income down by source. Productivity modifier (item 7) and starter coaching (item 10) deferred — productivity has design doc with 4 open questions; starter coaching needs a storyboard.
- **2026-05-06** — More grass-tile variety. Three orthogonal axes from the same deterministic tileHash: 4 noise variants (nv0..nv3, unchanged), NEW 4 base-color variants (gc0..gc3 — palette widened beyond the single #223322), and 16 decoration variants (gv0..gv15, doubled from 8) covering ~75% of tiles. Added 8 new patterns: dirt patch, dandelion, daisies, pebbles, clover, red poppies, bare-dirt patch, mossy log. Coverage gate and variant selector are on different bit fields so all 16 variants are reachable.
- **2026-05-06** — Buildings reference modal. New "?" button in the topbar opens a help dialog listing every active building grouped by section (Infrastructure / Civic & Services / Timber / Stone / Clay / Iron Industry). Each building is a click-to-expand row showing inputs (with rates), output (with rate), worker cost, build cost, road requirement, plus category-specific rows (booster radius/multiplier, police coverage/upkeep, food extractor tile placement, housing's tier chain). Closes on × button, click outside, or Escape. Standalone module — does not depend on or affect placement state.
- **2026-05-06** — Halved resource density on newly-allocated chunks. `_seed_resource_clusters` walks shortened: industry 4 × U(6,15) ≈ 42 tiles → 4 × U(3,8) ≈ 22 tiles, food 2 × U(4,8) ≈ 12 tiles → 2 × U(2,4) ≈ 6 tiles. Cluster count preserved (still 4 industry / 2 food patches per chunk) so the "discoverable patches" feel stays — patches are just smaller. Existing chunks not touched; players keep what they have. New patch in `migration_patches/halve_resource_density.sql`.
- **2026-05-06** — Recurring trade agreements between players. New `trade_agreements` table + 5 RPCs (propose / accept / cancel / list / `_pp_run_agreements`). Lifecycle: propose → accept → fires every N min → cancel. Resolver is single-driver (only the proposer's tick fires it) with `FOR UPDATE SKIP LOCKED`, fires at most one agreement per call (bounded backlog), skips firings when either side is short on stock or money (last_fired_at not advanced — retries next tick). Frontend: trade dialog gets a "Make recurring" checkbox + interval input that switches the Send button to call `propose_trade_agreement`; Players sub-tab gets a new "Recurring Agreements" section listing pending proposals (Accept/Reject for incoming, Cancel for outgoing) and active running agreements (End button). 14 new regression tests; full trade suite (35 tests) green.
- **2026-05-06** — Animations Pass 2 finished for the 9 buildings I'd left on a generic worker bob in Pass 1: foresters_office (leaf flutter, act-buzz), foreman_office (desk-lamp glow, act-glow), clay_master_hut (pottery wheel, act-spin), mine_office (lantern glow), mosaic_workshop (multi-color tile glint), engineer_workshop (rotating gear), distillery (copper still glow under the steam), brewery (golden tankard glint, --bldg-fig-* scaled for the 2×1 footprint), cabinetmaker (wood shavings sparks). Reuses existing keyframes — no new @keyframes added. Existing `.idle::before/.idle::after { animation: none !important }` already suppresses these when the building is paused / unstaffed / no-road / missing inputs.
- **2026-05-06** — Build panel sub-sections now accordion: opening one collapses the others. Storage migrated from per-section booleans (`city_build_sections_collapsed`: {infra:false, industry:false, …}) to a single string (`city_build_section_open`: "industry"). First load defaults to Infrastructure open. Empty string means "user explicitly closed everything". Old localStorage key becomes vestigial but harmless.
- **2026-05-06** — Build panel descriptions now show every input (including `input_resource_key_2`) for cross-recipe T4 processors — cabinetmaker (furniture + lime), architect (statuary + glass), mosaic_workshop (tiles + nails), engineer_workshop (tools + charcoal). Fix for the bug Atlas reported where a player had 174 furniture but no lime and the description gave no hint why cabinets weren't producing. Also added "Needs road access" to extractor / food_extractor / booster descriptions for parity with processors.
- **2026-05-06** — Rebaselined `baseline_schema.sql` from the live PG 17.6 Supabase DB. 51 patches accumulated since the last baseline (post-M2: multi-tile buildings, food chains, happiness, trade progression, crime + police, progressive unlocks, cash ledger, …) folded in via `pg_dump --schema-only` + `pg_dump --data-only` for the 6 catalog tables, spliced with a `DROP SCHEMA IF EXISTS public CASCADE` preamble for idempotent re-runs. Required PG17 client tools (`postgresql-client-17` from PGDG apt repo) since pg_dump rejects cross-major dumps. New baseline: 6,181 lines (was 2,400) — 22 tables, 66 functions, 27 indexes, 30 RLS policies, 268 GRANTs, 136 seed rows. All 51 patches moved to `migrations-archive/`; `migration_patches/` cleared with a refreshed README documenting the inbox workflow + rebaseline procedure. 296/296 tests green post-move (rebaseline doesn't touch the live DB; baseline is only loaded for fresh-project setup).
- **2026-05-05** — Replaced pinch-to-zoom with explicit +/− buttons (bottom-right of the map). iOS Safari's native pinch-zoom kept leaking through every preventDefault trick we tried (touch-action: pan-x pan-y on the viewport, gesturestart/change/end document-level preventDefault, touchstart passive:false 2-finger preventDefault) — symptom on Jill's iPhone was that pinching out wouldn't fully reset and walkers visibly drifted because Safari's native page-zoom and the app's pinch-zoom were both applying simultaneously. Buttons sidestep the whole gesture-ownership fight; pinch is now blocked but inert. ~80 lines net negative in map.js (ripped pinch state vars, touchDistance/touchCenter helpers, pinch zoom event handlers; kept the gesture* preventDefault listeners and 2-finger touchstart preventDefault as belt-and-suspenders to keep Safari from native-zooming the canvas).
- **2026-05-05** — Crime + police system. Per-player crime number (0–100) driven by uncovered active housing + size pressure + tavern count. Three police buildings — Watch House ($300/5w/r4/$5min), Police Station ($700/10w/r6/$12min, gated at tier 3 housing), Constabulary ($1500/15w/r8/$25min, gated at tier 5). Coverage requires the PD to have `is_staffed=true` (closes the original exploit where unstaffed PDs avoided upkeep but still gave coverage). New buildings.is_staffed column refreshed each tick by `_pp_staff_buildings`. Crime feeds happiness via `-floor(crime/5)` so a crime-ridden city slowly emigrates citizens. Upkeep deducted via new `_pp_run_upkeep` and logged as `'upkeep'` rows in cash_transactions. New topbar 🚨 indicator with green/amber/red color coding. 10 regression tests including the worker-shortage exploit. Design captured in docs/CRIME.md.
- **2026-05-05** — Cash ledger + evolve-housing devolve fix. New `cash_transactions` table with `_pp_run_tax` / `place_building` / `expand_district` writing rows for every money movement. Treasury sub-tab merges these into its earned/spent breakdown so it now shows Tax Revenue, Building Construction, and District Expansion alongside trade flows; the "tax + build costs aren't tracked yet" hint is gone. RLS scopes cash_transactions to the owning player. Also fixed a regression introduced by the progressive-unlock migration: the rewritten `_pp_evolve_housing` only re-checked road/well/food in the devolve branch, so Mansions no longer devolved when luxury food ran out — restored the full prereq-aware devolve while keeping the watermark bump on the upgrade branch. 4 new ledger tests + conftest.make_player defaulted to unlock_all=True so unlock-gate doesn't trip incidental tests.
- **2026-05-05** — Progressive building unlock by housing tier. Buildings whose only purpose is gating a higher housing tier are locked until the player reaches the previous tier. Sticky — once unlocked, stays unlocked even if the qualifying house devolves. Tracked via new `building_types.unlocks_at_housing_tier` and `player_profiles.highest_housing_tier_ever` columns. Server-side gate in place_building rejects with "Locked: X — reach housing tier N first"; frontend renders the row in disabled state with the same hint. _pp_evolve_housing bumps the watermark on every upgrade event. Seeded thresholds: school=3, temple=4, distillery/curing_house/spicery/brewery=5, cabinetmaker/architect/mosaic_workshop/engineer_workshop=6. 6 regression tests pin the gate behavior.
- **2026-05-05** — Deep code-review pass. (a) Real bug in `compute_happiness`: the staffing-ratio computation overwrote v_staffed and ended up measuring road connectivity, not staffing — replaced with a clean `worker_capacity / workers_needed` ratio that reuses `_pp_workers_needed`. (b) Land expansion cost bumped $500 → $1000 base (so 2nd chunk = $1000, 3rd = $4000, etc.) per Atlas's earlier call; server + both client previews updated. (c) panels.js `colors` + `spriteIcons` (~150KB of SVG data URLs) were being rebuilt inside the forEach loop on every renderBuildPanel; hoisted to module scope. (d) STRUCTURE.md updated to reflect population/happiness model + new docs (TRADE_PROGRESSION.md, HAPPINESS.md, TODO.md) + players.js dependency. (e) Renamed `test_population_drifts_up_when_happy` → `test_population_snaps_up_to_housing_capacity` to match the asymmetric model where immigration is happiness-independent, and added a regression test pinning the new staffing-ratio formula. 276 / 276 passing.
- **2026-05-05** — Trade tab: Resources + Treasury sub-tabs (replaces the thin Stats sub-tab). Resources shows one row per resource — colored dot, name, production rate (signed units/min), current stock, net trade $ — clickable to expand a per-partner breakdown with a "Trade" button on each player row that opens the existing trade dialog prefilled. Treasury shows earned / spent / net plus income-source and spending breakdowns by partner. Both share a Today / Week / All-time toggle and pull from trade_transactions + accepted player_trade_offers + a follow-up player_profiles lookup for counterparty names. Tax revenue and build costs aren't tracked yet — those land when a cash-ledger schema arrives (TODO).
- **2026-05-05** — Immigrant + emigrant walkers. When floor(population) changes between ticks, spawn matching walkers (capped at 3/tick). Immigrants spawn at a random border cell, walk across grass to the nearest road, follow roads via BFS to a road tile adjacent to a random active house, then step into the house. Emigrants reverse the path. Each walker rolls a random overlay — luggage / backpack / bindle / plain — rendered as a small SVG ::before on the citizen sprite. Pure visual; server population is unchanged.
- **2026-05-05** — Happiness system: per-player happiness (0–100) drives population dynamics. New columns on player_profiles (population, happiness, last_population_tick_at). compute_happiness scores 30 base + service / housing / food-variety / staffing inputs minus tax pressure. Population dynamic is asymmetric — fills target instantly when housing > pop (so build-house-get-workers feels right), but slowly emigrates when happiness < 50. worker_supply now derived from floor(population) + tavern_bonus instead of housing-supply directly. Topbar gets a smiley-scale happiness indicator (☹ / 😐 / 🙂 / 😊) with tooltip showing current immigration/emigration rate. Existing players seeded with population = max(5, worker_capacity) so the migration is invisible. 5 regression tests added; pre-existing test_boosters flake (random-tile placement) unrelated. Design in docs/HAPPINESS.md. Immigrant/emigrant walkers (visual flavor) deferred to a follow-up. Atlas asked for a separate productivity-modifier system; queued in Up next.
- **2026-05-05** — Round the "+N goods produced" toast: `total_produced` is a numeric (per-minute × elapsed seconds), so the toast was showing fractional values like 1.005377. Floor to whole goods and skip the toast entirely when the floor is 0; sub-1 production carries into the next tick anyway.
- **2026-05-05** — Missions tab now lists quiet traders with their next-request ETA. `get_active_missions` returns `{open, quiet}`; the Missions sub-tab renders open missions as before plus a "Waiting on next request" section underneath listing every active trader that's mid-cooldown.
- **2026-05-05** — Trade progression overhaul: NPCs no longer auto-available at start; player-to-player trade is the only option until the unlock gate is met (≥1 active extractor + ≥1 active food extractor + ≥1 tier-1 house). Once unlocked, the city's NPC trader pool is accessible — *city* = all districts in the world combined, *district* = one player. Each trader periodically posts an open city-wide request; players donate from inventory, faster fulfillment + larger contribution = more reputation. Personal reputation per (player, trader) feeds into a city reputation that's a population-weighted average across all districts. City rep tiers (50/150/400/1000) gate trader inventory expansion. Reputation decays 2%/day if no donations in the last 7 days. Black market sell prices cut by ~35% so the gate has tangible value. Trade tab restructured into Partners / Missions / Players / Stats sub-tabs (top-level Players tab dropped). Locked-state UI shows the three requirements with check marks. Stats sub-tab summarizes today/week/all-time imports/exports/balance and top partners. Schema: trader_relationships, trader_missions (unique partial index forces ≤1 open per trader), trader_mission_donations. New RPCs: is_trade_unlocked, district_weight, city_reputation, city_rep_tier, roll_trader_missions, expire_old_missions, donate_to_mission, decay_reputations, get_active_missions, get_trade_partner_view, get_trade_stats. 8 new regression tests; 269 / 269 passing. Design captured in docs/TRADE_PROGRESSION.md.
- **2026-05-05** — Build panel: split Industry section into Industry + Farming. Resource chain (extractor / non-food processors / cross-converters / T4 cross-recipes / resource boosters) stays in `<Industry>`; food chain (food extractor / food-output processors / luxury food / food boosters) moves to a new `Farming` section. Routing distinguishes food vs non-food via the output resource's `is_food` flag and the booster's `boost_target`.
- **2026-05-05** — Build panel: collapsible sections — Infrastructure (road + house), <Industry> Industry (industry-locked production), Civic & Services (services + tax). Replaces a flat 15+ item list. Sections sort by category + tier within; collapse state persists per-section in localStorage so the panel layout survives reloads.
- **2026-05-05** — Toast z-index lifted to 300 so notifications paint above the trade dialog (200) and inspector overlay (150). Send-Offer validation messages were getting hidden behind the dialog they referred to.
- **2026-05-05** — Trade UX overhaul: offer-card wording is now viewer-perspective ("You'd give:" / "You'd receive:" + "From X" / "To X" header) for both inbox and outbox; inbox pre-checks money + inventory and disables Accept with a "Can't accept: missing 5 lumber" notice when you can't fulfill; new `get_player_trade_view` RPC + dialog wiring shows the counterparty's tradeable inventory ("they have N" annotations + dropdown filtered to what they actually hold) and the Send button refuses to ask for more than they have.
- **2026-05-05** — Persist map view (scroll + zoom) per player to localStorage, restored on game reopen so you land where you were last looking instead of at (0,0). Falls back to centering on the player's home tile for a first-ever load. Saved zoom is clamped against the current dynamic minimum so a stored zoom from a smaller district adapts gracefully.
- **2026-05-05** — Building animation audit + Pass 1 + Pass 2. Audit confirmed the issue-suppression contract was already correct (`map.js` adds `.producing` only when staffed + road-connected + fed + unpaused; `.idle::before/::after { animation: none !important }` kills any pseudo-element animation when off). 32 buildings had no `.producing` animation; Pass 1 filled all of them with the established `act-bob` (worker overlay) and `act-smoke` (chimney puff) patterns, tinted per-building (iron worker dark, lime smoke white, glassworks smoke gold, etc.). Pass 2 layered distinctive motion on 11: lantern glow at iron mine, forge glow under smelter & glassworks, sparks off nail forge, a bee weaving across apiary, a fast-pecking chick at hatchery, water shimmer flowing in irrigation channel, water ripple at fishing pier line, full-building sway for orchard / garden / winery. New keyframes: act-glow, act-buzz, act-flow, act-sparks, act-ripple. Multi-tile buildings (tax_man, brewery) wrap position/size in `--bldg-fig-*` so figures stay 1×1-equivalent.
- **2026-05-05** — Trade-offer label fix (outbox view). `renderOfferCard`'s outbox path inverted "You give" / "They give" verbs while also swapping the underlying data, so an offer you sent showed up with the labels reversed. Rewrote the mapping with the sender-perspective DB contract spelled out and inlined the static labels. Inbox already rendered correctly and is unchanged.
- **2026-05-05** — Extract `js/map_roads.js` from `map.js` (1135 → 890 lines). Road autotile SVG generator + placement-time `roadTileSet` cache + connectivity helpers (`getRoadTileSVG`, `rebuildPlacementRoadSet`, `isRoadPlacementConnected`, `roadNeighborFlags`) now self-contained behind a small surface area. Walker module's road graph stays in `walkers.js` (different concern: pathfinding vs. placement validation). Updated `STRUCTURE.md` + the `index.html` cache-buster module list. Removed unused `isRoadAdjacent` while moving.
- **2026-05-05** — `place_building` worker-math drift fix. The function's inline `workers_needed` query was missing `food_extractor` + `booster` categories and didn't require road access for extractors, so the response immediately after placing a building under-reported worker usage until the next `process_production` tick corrected it. Replaced with a shared `_pp_workers_needed(uid)` helper that mirrors `_pp_staff_buildings`'s rules — they can no longer drift. Added regression test that places an orchard then a timber camp and asserts `workers_needed` includes both.
- **2026-05-05** — Refactor `resolve_trader_visit` (212 lines) into orchestrator + 2 phase helpers (`_rtv_sell_phase`, `_rtv_buy_phase`). Same architectural pattern as `process_production`: thin orchestrator over named phases. Each phase independently testable; the orchestrator handles validation, the catch-up loop, money settlement between phases, and visit-row recording. 260/260 tests passing post-refactor.
- **2026-05-05** — Refactor `process_production` (470 lines) into orchestrator + 11 phase helpers (`_pp_compute_worker_supply`, `_pp_housing_supply`, `_pp_tavern_bonus`, `_pp_workers_needed`, `_pp_staff_buildings`, `_pp_run_extractors`, `_pp_run_food_extractors`, `_pp_bump_boosters`, `_pp_run_processors`, `_pp_run_services`, `_pp_run_tax`, `_pp_drain_housing_food`, `_pp_evolve_housing`). Tavern bonus is now captured ONCE at the start of the tick — re-evaluating after the service loop would always succeed because `last_processed_at` gets updated mid-tick (zero elapsed → zero need → trivially met). Establishes the `_pp_*` / `_rtv_*` prefix convention for future PL/pgSQL refactors. 260/260 tests passing.
- **2026-05-04** — Multi-tile buildings + walker variety. Two changes shipped together. **Multi-tile**: `building_types.footprint_w / footprint_h` (default 1×1, max 4×4). `place_building` validates and claims every tile in the footprint; `map_tiles.occupied_building_id` UNIQUE constraint dropped (multiple tiles can share); demolish needs no extra code (FK ON DELETE SET NULL cascades). Frontend renders the anchor's .bldg with absolute positioning + 200%/300% sizes spanning the footprint; interior tiles route clicks back to the anchor via `data-anchor-x/y`. First multi-tile picks: tax_man (2×2), school (2×2), temple (2×2), brewery (2×1). **Walker variety**: WALKER_MAX_COUNT now scales `min(80, 8 + 0.6 × eligible_spawn_count)` so big cities feel populated. Citizen ambient walkers pick a weighted persona — child / elder+cane / couple / pet-walker / merchant / caped / umbrella / hatted — for visual character without 30 sprite variants. New CSS for 3 sprite variants (walker-child / walker-elder / walker-couple) and 5 overlay classes (has-cane / has-pet / has-pack / has-cape / has-umbrella). Tests: 10 multi-tile cases + 10 housing-tier tests + 1 luxury-chain test updated for the larger footprints. 234/234 passing.
- **2026-05-04** — Food tiles. Food extractors (Orchard / Fishing Pier / Garden / Grain Farm) must now be placed *on* a matching food tile (orchard_grove / pond / garden_plot / farmland). Adds 4 new `kind='terrain'` rows to `resources` (not tradeable goods, just FK targets for `map_tiles.resource_node_key`); a `placement_resource_node_key` column on `building_types`; an updated `reject_build_on_resource` trigger that enforces the positive requirement *and* the existing block on every other building. Tile is NOT consumed at placement — demolish reveals it again. `clear_resource_tile` additionally rejects occupied tiles. Cluster seeding extracted into a shared `_seed_resource_clusters` helper; `allocate_district_chunk` now drops 4 ore clusters (6-15 walks each, unchanged) plus 2 food clusters (4-8 walks each). Industry-locked food tiles match ore — a stone player only gets ponds. Existing chunks back-filled by the migration's DO block. New SVG sprites (pond with reeds + lily pads, plowed farmland with stake, walled garden plot with vegetable rows, orchard grove with apple/golden fruit). Inspector adds "Build here" row + terrain-aware copy. Catalog description shows the matching tile requirement. Existing food-extractor tests updated via a new `stamp_food_tile` fixture; choose_industry test bound bumped to accommodate the now-larger cluster variance. 20 new tests; 224/224 passing.
- **2026-05-04** — More housing tiers (6/7/8 — Mansion / Estate / Palace). Caps now extend to t8, with workers ladder 50/70/100 (50/70/100 worker yield; up from 34 at t5). Three new prereq columns on `housing_tier_config`: `needs_luxury_food` (t6+), `needs_industrial_luxury` (t7+), `needs_all_industrial_luxuries` (t8 only). Resource flags `is_luxury_food` (spirits/caviar/spices/ale) and `is_industrial_luxury` (cabinets/monuments/mosaics/machinery). The "all 4" gate is a count comparison (in-stock ≥ total defined) so it generalizes if more industrial luxuries are added later. T8 effectively requires a fully-traded city across all four industries. Inspector blockers + descriptions wired; new SVG sprites for t6/t7/t8 (cupola, walled compound + spired towers, central dome + columned portico). 13 new tests; 204 / 204 passing. Booleans-per-prereq scheme is now at 8 columns — flagged in CLAUDE-time-future as JSON-over-columns once we exceed ~10.
- **2026-05-04** — Phase C2: Advanced cross-recipe T4 buildings. Closes the strategic-depth expansion arc. Each industry has a T4 processor consuming its own T3 output PLUS another industry's cross-good — round-robin: timber needs stone's lime, stone needs clay's glass, clay needs iron's nails, iron needs timber's charcoal. Cabinetmaker (cabinets), Architect (monuments), Mosaic Workshop (mosaics), Engineer's Workshop (machinery). $700/10w, 0.5+0.5 → 0.25. The trade-pressure unlock: every industry's top-tier production now genuinely requires another industry's good. Side-fix: tier CHECK widened from {1,2,3} to {1,2,3,4}. 19 new tests; 191 / 191 passing.
- **2026-05-04** — Phase C: cross-converters + multi-input processor support. 4 new buildings (Charcoal Kiln, Lime Kiln, Glassworks, Nail Forge) producing 4 new support goods (charcoal, lime, glass, nails). Each industry has one cross-converter producing its unique cross-good. The processor loop in `process_production` was extended to support a 2nd input — output is gated by `min(avail/need) across all inputs`, both inputs drain proportional to that progress. Existing single-input processors unchanged. Phase C2 (advanced cross-recipe T4 buildings) split into a follow-up so this commit stayed focused. 17 new tests; 172 / 172 passing.
- **2026-05-04** — Luxury T3 chain extensions (Phase B of the strategic-depth expansion). Each industry's T2 food upgrades into a luxury good: Distillery (wine→spirits), Curing House (smoked_fish→caviar), Spicery (preserves→spices), Brewery (flour→ale). $500/10w/0.5→0.25 each. All four luxury outputs are `is_food=true` so they satisfy the housing food gate; their real strategic value is as high-margin trade goods and the natural prereq for future high-tier housing. Brewery shares flour with Bakery — player chooses (via priority/pause) between commodity bread and luxury ale. 14 new tests; 155 / 155 passing.
- **2026-05-04** — Production boosters (Phase A of the strategic-depth expansion). Each industry has a resource booster + a food booster, all `category='booster'` with new columns `boost_target` ('extractor'/'food_extractor'), `boost_multiplier` (default 1.25), `boost_range` (default 2). Staffed boosters apply MAX multiplier (no stacking) to matching adjacent buildings within range. 8 buildings: Forester's Office, Apiary (timber); Foreman's Office, Hatchery (stone); Clay Master's Hut, Compost Heap (clay); Mine Office, Irrigation Channel (iron). $200 / 3 workers each. 11 new tests. 141 / 141 passing.
- **2026-05-04** — Industry tree buildout. Two-category architecture (per Atlas's framing): industry/food trees are per-industry; housing / road / services / tax are general. Locked existing chain buildings (clay_pit, pottery_kiln → clay; mill, bakery → iron). Filled the tree-shape gaps with 6 new buildings: Smelter (iron T2: iron → iron_ingot), Toolmaker (iron T3: iron_ingot → tools), Tile Maker (clay T3: pottery → tiles), Winery (timber food T2: berries → wine), Smokehouse (stone food T2: fish → smoked_fish), Cannery (clay food T2: vegetables → preserves). 6 new resources matching. Wine, smoked_fish, preserves all `is_food=true` → auto-satisfy the housing food gate. Result: every industry has the same tree shape (T1 extractor / T2 processor / T3 processor + T1 food / T2 food), just with different resources. Cross-conversion (multi-input recipes that take resources from another industry's chain) deferred as a follow-up. 24 new tests; 130 / 130 passing.
- **2026-05-04** — Iron replaces grain as the 4th industry. Grain is now a food only (it always was `is_food = true`; just no longer one of the choosable industries). Iron has its own primary extractor (`iron_mine`, tile-based like the others) and uses `grain_farm` as its paired food extractor (flat-rate, locked to industry='iron'). Atlas's existing grain_farm grandfathered in (still produces; he just can't build new ones). Industry-pick UI now shows all four cards (was missing iron and clay). 7 new tests; 106 / 106 passing.
- **2026-05-04** — Paired food extractors. Each industry has a flat-rate food extractor: **Orchard** (timber → berries), **Fishing Pier** (stone → fish), **Garden** (clay → vegetables); grain industry already had `grain_farm`. New `food_extractor` category in `building_types`; new flat-rate production loop in `process_production` that mirrors the extractor loop minus the path math. Food extractors don't need road access (like extractors). All three new resources marked `is_food = true` so they auto-satisfy the housing food gate. 9 new tests; 98 / 98 passing.
- **2026-05-04** — Tavern bonus is no longer decorative. `v_tavern_bonus` is now pre-computed BEFORE the staffing loop based on each tavern's `(active + road + inputs cover the elapsed tick)` qualification, then added to `v_worker_supply` so the bonus actually buys staffing slots. Net effect: a fed tavern (cost 5, bonus 10) gives a real net +5 staffable workers. Edge case noted in the migration: a tavern that qualifies on inputs but doesn't make it into `v_staffed_ids` would over-count by 10 — rare in practice, accepted vs. an iterative staffing solver. Two new regression tests pin the fix: one shows fed tavern lets an extractor staff that wouldn't otherwise; one shows unfed tavern correctly doesn't pre-boost.
- **2026-05-04** — Housing food consumption per tick. Tier 1+ houses drain food at 0.03 / 0.06 / 0.10 / 0.15 / 0.25 food/min/house (config in `housing_tier_config.food_per_minute`). Drain proportional across all `is_food` resources via single multiplier — no one food unfairly drained first. Player-level `last_food_tick_at` tracks elapsed (separate from per-house `last_processed_at` so the upgrade-secs timer keeps working). When food runs out, the existing devolve gate fires (unless a fed bathhouse is in range). Inventory panel rate display shows the food drain split across foods proportionally.
- **2026-05-04** — Staffing priority + pause. Each worker-consuming building has `staffing_priority` (Low/Normal/High, default Normal); inspector exposes pills + a Pause toggle. Server staffing loop sorts `priority DESC, created_at ASC`. Paused buildings drop out of every `process_production` loop via the existing `status='active'` filter — no per-loop change needed; resume resets `last_processed_at` so paused time isn't dumped into production. Side-fix: `next_starter_row` was picking `max(reserved_row)+1` without checking whether the corresponding `(0, row)` chunk had been claimed by another player's expansion — Atlas had expanded into row 1, breaking new-player allocation. Now walks forward to the first row whose `(0, row)` is unoccupied.
- **2026-05-04** — Housing food gate. Tier 1+ requires "any food" in inventory (presence-only check, not consumption — switch to per-tick drain after the food-pairing item lands so non-grain players have a local food source). Foods flagged via `resources.is_food`; today grain, flour, bread qualify. Tier 0 shanty stays free as the subsistence floor. Devolve fires on starvation unless a fed bathhouse is in range. 6 new tests; 4 existing tier-1+ housing tests stocked grain to isolate their original concerns.
- **2026-05-04** — Inspector "Issues" section. Replaces scattered Road/Workers/Status rows with one consolidated list — every reason a building isn't operational, each with a fix hint. Status row above shows "Operational" or "N issues". Housing upgrade gating now mirrors all server prereqs (road, well, food, school, temple) so the "Next: Conditions met" message is honest. Also fixed `computeRoadAccess` to track service/tax road access (was silently treating them as no-road).
- **2026-05-04** — Service walkers + staffed/unstaffed visual states. Each new service (tavern, bathhouse, school, temple) spawns its own walker sprite when staffed, and the on-map tile shows a producing animation only when staffed + road-connected. Faded sprite + red `!` when unstaffed.
- **2026-05-04** — Citizen-service buildings (×4), each consuming a unique pair of resources to push trade. **Tavern** (bread+pottery → +10 worker capacity, $350/5w/tier 2), **Bathhouse** (brick+clay → blocks housing devolve in 4-tile radius, $300/5w/tier 2), **School** (lumber+flour → gates Townhouse tier 3 in 5-tile radius, $600/10w), **Temple** (statuary+brick → gates Villa tier 4 in 6-tile radius, $800/10w). All require staffing AND ongoing input feeding to operate — drop either input → effect disappears next tick. Schema: added `input_resource_key_2`/`input_rate_2` to `building_types` (cheaper than a join table); added `needs_school`/`needs_temple` to `housing_tier_config`. Process: `process_production` now feeds services in a fourth loop, populates `v_operating_services`, and `v_has_school`/`v_has_temple`/`v_has_bathhouse` queries that set in the housing loop. Tavern's worker bonus uses the existing `output_rate` column (10). Inline-SVG sprites in the kiln style for all four. Tests: 7 covering multi-input charging, partial-feed idle, worker-capacity bonus on/off, tier 3 gate fed/unfed, devolve block fed/unfed.
- **2026-05-04** — District expansion: row-based starters + player-picked expansion. Each player reserves one row at signup; expansion candidates are unowned chunks orthogonally adjacent to the player's district excluding other players' reserved rows. Tap **+ Expand**, candidate chunks pulse gold, tap one to claim. Trapped state impossible because your own row's edges are always available.
- **2026-05-04** — Resource tile rules: can't build on a resource tile (BEFORE INSERT trigger). Tap an owned resource tile to clear it (sets `resource_node_key = NULL`); then it's buildable. Free for now; demolish blocked if an extractor still targets the tile. Tests, including a `clear_resources` shared fixture for tests that build at known coords.
- **2026-05-04** — Wider zoom-out as the map grows. New `computeMinZoom()` returns the smaller of `MAP_MIN_ZOOM` (0.5, the existing static floor for small maps) and the dynamic "fit the whole grid in the viewport" zoom. Hard floor at 0.05 so tiles don't disappear on very large districts.
- **2026-05-04** — Hide tile coordinates in inspector panels. Stripped the `(x, y)` row from buildings, walkers, and the new resource-tile inspector. Walker also lost its redundant duplicate-source row in the cleanup.
- **2026-05-04** — Resource tile inspector. Tapping an owned resource tile now opens the same panel chrome the building inspector uses, with rows listing the deposit's role: harvested by which extractor, processed by which processor → output, optional second downstream step, and the tile coords. Demolish button at the bottom calls `clear_resource_tile` (matches the server's "blocked while an extractor is targeting this tile" rule with an inline warning). The old confirm dialog flow is gone.
- **2026-05-04** — Well polished: 3 workers + road requirement + walker. Staffing flows through the same loop as processors/tax (`category='service'` joined). New 64×64 inline-SVG art in the kiln/mill style — stone ring, water inside, two posts holding a small thatched roof, a bucket on a rope. Well attendants render as plain `citizen` walkers for now. Tax Office still uses the placeholder solid-color tile (its own SVG polish is a separate task).
- **2026-05-04** — Two new buildings: **Well** (`category='service'`, $50, 0 workers) is now a precondition for housing to evolve past tier 0 — needs to be within Manhattan distance 4 of the house. **Tax Office** (`category='tax'`, $300, 10 workers, needs road) credits $10/min when fully staffed via a new section in `process_production`. Placeholder solid-color tile art with letter labels (W / TX); SVG polish later. Happiness hookup deferred to that item.
- **2026-05-04** — Walker variety: WALKER_MAX_COUNT 12→7, spawn chance 0.4→0.22, max steps 18→14, cooldown 4→6 ticks. Per-walker visual jitter via CSS vars: `--wk-scale` (0.85–1.15), `--wk-hue` (±18°, hue-rotates the sprite), `--wk-bob-ms`/`--wk-waddle-ms` (small per-walker animation period jitter). ~18% of ambient walkers spawn `has-hat` → small brown-cap SVG anchored to the head. Same five base sprites, but every walker reads as a slightly different person.
- **2026-05-04** — Worker requirements bumped: extractors and processors all 10 workers each (was 2-4). The all-or-nothing per-building staffing gate already existed in `process_production` (a building only joins `v_staffed_ids` if remaining workers ≥ its worker_cost; only staffed buildings produce). The gate just rarely fired at the old low costs. Now early game is meaningfully tighter — likely future tuning on housing yields if the first-extractor wait feels too long.
- **2026-05-04** — Highway network + remove HQ tile. Every chunk gets a horizontal strip at y_offset=7 and a vertical strip at x_offset=7 stamped as `terrain_type='highway'`, unbuildable, no resource. Highway counts as cost-1 walkable for walker pathing (any owner) and as road-adjacent for `has_road_access`. Roads now connect to highway OR another of your roads (drops the old "adjacent to home" seed rule). City-center special tile removed entirely; `home_x/home_y` retained as logical anchor.
